import React from 'react';
import AdminResourceCategory from './AdminResourceCategory';

const MultimediaCollection = () => {
  return (
    <AdminResourceCategory
      category="multimedia"
      title="Multimedia Collection"
      description="Manage videos, audio, and interactive content"
    />
  );
};

export default MultimediaCollection;
